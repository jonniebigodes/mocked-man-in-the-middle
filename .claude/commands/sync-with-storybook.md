---
description: "Re-connect Storybook mcp settings"
allowed-tools: storybook-mcp - get-ui-building-instructions
---

## Your task

Reconnect the Storybook MCP settings by running the `get-ui-building-instructions` tool from the `storybook-mcp` command set. This will ensure that the MCP is synchronized with the current state of Storybook and its components.
